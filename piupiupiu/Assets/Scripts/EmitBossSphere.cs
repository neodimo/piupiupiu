using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EmitBossSphere : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //if (gameObject.name )
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
